# requirements.txt — Logi-Agri SN v2+
# Tested with Python 3.12.3 / Java 11 / Spark 3.3 on Windows
#
# Recommended install sequence:
#   py -3.12 -m pip install --upgrade pip setuptools wheel
#   py -3.12 -m pip install --use-deprecated=legacy-resolver -r requirements.txt
#
# Note: pyspark==3.3.2 is sourced as a tarball for Windows and may take several minutes to build.

# Core pipeline
pyspark==3.3.2
kafka-python==2.0.2

# Validation
pandera==0.20.4
pandas==2.2.2

# MLOps
apache-airflow==3.0.3
mlflow==2.14.0

# Stockage
pyhive==0.7.0
thrift==0.16.0
happybase==1.2.0

# Optimisation & Analyse
scipy==1.11.4
numpy==1.26.4
matplotlib==3.7.3
seaborn==0.12.2

# Tests
pytest==7.4.3
pytest-cov==4.1.0

# Installation :
# pip install -r requirements.txt
# Java 11 requis : sudo apt install openjdk-11-jdk
